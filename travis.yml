language: python
python:
  - "2.7"
  - "3.5"
# command to install dependencies
install:
  - pip install .
# command to run tests
script: 
py.test test_sudoku_solver_hard_cw_submission.py  -v --cov=. --cov-report term-missing